# CUDN EVPN/Netris VMaaS E2E profile -- infrastructure values for osac-deps
# and osac-infra charts.
#
# The CUDN EVPN/BGP/VTEP fabric is an external prerequisite. This file enables
# the same cluster prerequisites as the CaaS E2E profile; it does not create
# the external EVPN fabric itself.

# --- OLM Subscriptions (osac-deps) ---
certManager:
  enabled: true
trustManager:
  enabled: true
aapOperator:
  enabled: true
lvms:
  enabled: true
metallb:
  enabled: true
cnv:
  enabled: true
mce:
  enabled: false

# --- Infrastructure (osac-infra) ---
caIssuer:
  enabled: true
keycloak:
  enabled: true
  devFixtures:
    enabled: true
kafka:
  enabled: true

# --- Bundled PostgreSQL ---
# Single-pod ephemeral database for testing.
bundledPostgres:
  enabled: true
  database:
    name: service

# --- Bundled OpenBao ---
# Single-pod ephemeral Vault-compatible store for testing.
bundledVault:
  enabled: true
  devRootToken: "dev-root-token"
  podSecurityContext:
    runAsNonRoot: true
